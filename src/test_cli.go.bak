package main

import "fmt"

func main() {
	accounts, err := getAccounts("testpass")
	fmt.Println("Accounts:", len(accounts), "Err:", err)
	if len(accounts) > 0 {
		fmt.Println("First:", accounts[0])
	}
}
